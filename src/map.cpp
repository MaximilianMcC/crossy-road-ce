#include <graphx.h>
#include <time.h>
#include <debug.h>

#ifndef MAP

class Map
{
public:

	Map()
	{
		// Set the random seed as the current time
		srand(time(NULL));

	}

	// Draw the map to the screen
	void render()
	{

	}

	// Add a row to the map
	void addRow()
	{
		
	}



private:

};

#endif